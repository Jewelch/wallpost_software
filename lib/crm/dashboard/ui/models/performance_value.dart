import 'dart:ui';

class PerformanceValue {
  String label;
  String value;
  Color textColor;
  Color backgroundColor;

  PerformanceValue({
    required this.label,
    required this.value,
    required this.textColor,
    required this.backgroundColor,
  });
}
