import 'package:flutter/material.dart';

class ApprovalsListWidget extends StatelessWidget {
  const ApprovalsListWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
